[package]
name = "myapp"
version.workspace = true
edition.workspace = true

[dependencies]
gerax = { path = "../../crates/gerax", features = ["actix", "mongodb"] }
# gerax = { version = "0.1", default-features = false, features = ["axum", "mongodb"] }

tokio = { version = "1", features = ["full"] }